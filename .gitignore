# Pasta Escopos (uppercase): dados brutos do user — ZIPs (130MB AR, 82MB CO),
# HTMLs rudimentares (2.7MB), CSVs locais. Fica fora do repo.
# Os GeoJSONs extraidos vivem em country-scopes-data/ (versionado).
/Escopos/

# Cache temporário do Claude Code
.claude/

# Scaffolding de build do "dashboard teste.html" (peças extraídas do bundle
# do Claude Design + backup do export original). O HTML final é self-contained.
.tmp_extract/

# Arquivos locais de apoio / material pra mostrar pro Claude — NUNCA publicar.
# Repo é PÚBLICO e tem PII de motorista (nomes/emails nos PDFs), dados
# operacionais (CSVs) e imagens soltas. Fica tudo só no PC do Lucas.
Prints/
VID_Status.csv
Gemini_Generated_Image_*.png
true pgn globe.png
